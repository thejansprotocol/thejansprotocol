# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json # Optional, some prefer to commit it, others don't. If you use npm, commit package-lock.json. If yarn, commit yarn.lock.
# yarn.lock # Uncomment if using Yarn and you prefer not to commit it (though usually committed)

# Environment variables
.env
*.env
.env.*
!.env.example # It's good practice to have an example env file

# Operating System Files
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# Build Output / Temporary Files (if any, you might not have these yet)
dist/
build/
out/
coverage/
*.tmp
*.swp
*.swo

# Logs (general runtime logs, if your top-level 'logs/' dir is for this)
logs/
*.log
# Exclude specific log directories if they contain valuable historical data you *do* want to track,
# but generally, runtime logs are ignored. Your frontend/data/dailylogs_v8/ are data, not runtime logs.

# Private data for backend scripts
backend_scripts/**/private_data/
# More specific if you prefer:
# backend_scripts/automator/private_data/pending_rounds/
# backend_scripts/automator/private_data/pending_rounds_v7/
# backend_scripts/automator/private_data/pending_rounds_V8/

# IDE / Editor specific files
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Oracle JDK (This should not be in your repository)
Oracle_JDK-24/

# Any other sensitive files or large binaries
# For example, if you had large media files not suitable for Git:
# large_videos/